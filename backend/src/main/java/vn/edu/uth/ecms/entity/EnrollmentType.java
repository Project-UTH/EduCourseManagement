package vn.edu.uth.ecms.entity;

/**
 * Type of enrollment
 */
public enum EnrollmentType {
    NORMAL,     // Đăng ký bình thường (tự đăng ký trong thời gian mở đăng ký)
    MANUAL      // Admin thêm thủ công (học bù, chuyển lớp, đăng ký muộn)
}